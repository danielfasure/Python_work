#need create a url a url is where the database will be stored for a basic sql using sql lite 3
# sqllite url goes like this #'sqlite:///./(name_of_database).db'

# enginge allow you to open a connection to that location and you can also pass along some rule for example only one thread at a time
#the engine come from sqlalchemy a package you have to install into your env
engine = create_engine(( variable_name_where_url_is_stored),(connect_args= {'check_same_thread:False }))

#you now need a session this like a factory that you use that allow you read and write your database
#you pass in session maker and call an instance of that class
#SessionLocal = sessionmaker(autocommit =False, autoflush = False, bind = engine )
first two parameter means wheneter or not addition or deletion is autosaved and the latter mean what engine is being used to operate this procedure is auto saved


#a database file also provide a template maker for what the database table should follow
#the databse file need an instance of the declarative_base class
#Base = declarative_base()
#this is then used by the class that are model to then creature the struture that the database has to follow

class user_model (Base)
 __tablename__ =