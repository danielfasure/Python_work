#import an hashing algorithm that has an method 
#you save the hash password to the database 
#when you what to verify password the user pass in the password the hash algorithm is applied an if it mashes then it authethicate the user 
#brcrypt is used and produced a long string of text and that is pass onto the database not the user actual password 
#you import the passlib.context pakacage and import the cryptcontext
#you then create an instance of that class it take in two parameter 
#first is the type of hashing algorithm so in my case brcypt context.
#latter is deperacte value which i am setting to zero to avoided issues 

#need pass lib and brcrypt installed for it to work 